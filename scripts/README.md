# Scripts

Place new yk-FERTA scripts here.

Examples:

- dataset import / cleaning
- local evaluation entrypoints
- migration utilities
- one-off offline indexing jobs

Avoid adding new product logic to the legacy root-level shell scripts.
